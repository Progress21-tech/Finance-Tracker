// Checkpoint 2 — will be replaced with the full landing page
export default function Landing() {
  return (
    <div className="min-h-screen bg-[var(--bg)] flex items-center justify-center">
      <p className="text-[var(--text-muted)]">Landing page — coming in checkpoint 2</p>
    </div>
  );
}
